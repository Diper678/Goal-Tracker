---
phase: 02-ios-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/public/icon.svg
  - app/scripts/generate-icons.mjs
  - app/pwa-assets.config.ts
  - app/public/apple-touch-icon.png
  - app/public/pwa-192x192.png
  - app/public/pwa-512x512.png
  - app/index.html
  - app/vite.config.ts
autonomous: true
requirements: [IOS-02, IOS-03]

must_haves:
  truths:
    - "User sees a branded splash screen on PWA launch instead of white flash"
    - "User sees a gold trophy app icon on the iOS home screen"
  artifacts:
    - path: "app/public/icon.svg"
      provides: "Trophy SVG source icon with correct xmlns namespace"
      contains: "xmlns=\"http://www.w3.org/2000/svg\""
    - path: "app/public/apple-touch-icon.png"
      provides: "180x180 PNG for iOS home screen"
    - path: "app/public/pwa-192x192.png"
      provides: "192px PWA manifest icon"
    - path: "app/public/pwa-512x512.png"
      provides: "512px PWA manifest icon"
    - path: "app/scripts/generate-icons.mjs"
      provides: "Node script to convert SVG to PNGs via sharp"
    - path: "app/pwa-assets.config.ts"
      provides: "Config for @vite-pwa/assets-generator splash screens"
    - path: "app/index.html"
      provides: "apple-touch-icon link, splash screen links, viewport-fit=cover, apple-mobile-web-app-title"
    - path: "app/vite.config.ts"
      provides: "Updated manifest with PNG icon entries"
  key_links:
    - from: "app/public/icon.svg"
      to: "app/public/apple-touch-icon.png"
      via: "sharp in generate-icons.mjs"
      pattern: "sharp.*resize.*180.*180"
    - from: "app/pwa-assets.config.ts"
      to: "app/public/apple-splash-*.png"
      via: "@vite-pwa/assets-generator CLI"
      pattern: "combinePresetAndAppleSplashScreens"
    - from: "app/index.html"
      to: "app/public/apple-touch-icon.png"
      via: "link rel apple-touch-icon"
      pattern: "apple-touch-icon"
---

<objective>
Create the branded trophy app icon and iOS splash screen assets, update index.html with all required iOS PWA meta tags and asset links, and update vite.config manifest.

Purpose: Users currently see a broken icon (malformed SVG namespace) and a white flash on PWA launch. This plan replaces the icon with a gold trophy on dark gradient and generates all splash screen PNGs so iOS displays a branded launch screen.

Output: Trophy SVG, generated PNG icons (180, 192, 512), generated splash screen PNGs, updated index.html with all iOS meta/links, updated vite.config manifest.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ios-platform/02-CONTEXT.md
@.planning/phases/02-ios-platform/02-RESEARCH.md

@app/index.html
@app/vite.config.ts
@app/public/icon.svg

<interfaces>
<!-- Current index.html has these iOS meta tags already: -->
<!-- <meta name="apple-mobile-web-app-capable" content="yes" /> -->
<!-- <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" /> -->
<!-- Missing: viewport-fit=cover in viewport meta, apple-touch-icon link, splash links, apple-mobile-web-app-title -->

<!-- Current vite.config.ts VitePWA manifest has only SVG icon entry -->
<!-- includeAssets references apple-touch-icon.png but file doesn't exist yet -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trophy SVG icon and generate PNG assets</name>
  <files>app/public/icon.svg, app/scripts/generate-icons.mjs, app/public/apple-touch-icon.png, app/public/pwa-192x192.png, app/public/pwa-512x512.png</files>
  <action>
1. Replace `app/public/icon.svg` with a new trophy icon SVG. The existing SVG has a malformed namespace (`xmlns="http://www.w3.org/20MathML"`) that must be fixed. The new SVG must:
   - Use correct namespace: `xmlns="http://www.w3.org/2000/svg"`
   - Have viewBox="0 0 512 512" width="512" height="512"
   - Background: dark gradient (#1E293B to #020617 radial) with rounded corners (rx="80")
   - Trophy shape in gold (#FACC15 — legendary rarity color): cup body, handles (left/right arcs), stem, base
   - Star accent inside cup in dark (#030712) for contrast
   - Per user decision: "Gold (legendary rarity color) icon element on dark background — premium feel"
   - Per user decision: "Subtle dark gradient — stands out on home screen"
   - Use the exact SVG from RESEARCH.md code examples section (Trophy SVG Icon)

2. Install dev dependencies in `app/` directory:
   ```bash
   cd app && npm install -D sharp
   ```

3. Create `app/scripts/generate-icons.mjs`:
   - Import sharp, path, fileURLToPath
   - Read `public/icon.svg`
   - Generate three PNGs:
     - `public/apple-touch-icon.png` at 180x180
     - `public/pwa-192x192.png` at 192x192
     - `public/pwa-512x512.png` at 512x512
   - Log success message

4. Run the script: `node scripts/generate-icons.mjs` (from app/ directory)
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Goal Tracker/app" && node scripts/generate-icons.mjs && test -f public/apple-touch-icon.png && test -f public/pwa-192x192.png && test -f public/pwa-512x512.png && echo "All icons generated"</automated>
  </verify>
  <done>Trophy SVG with correct namespace exists, three PNG icons generated at correct sizes (180, 192, 512)</done>
</task>

<task type="auto">
  <name>Task 2: Configure splash screens, update index.html and vite.config</name>
  <files>app/pwa-assets.config.ts, app/index.html, app/vite.config.ts</files>
  <action>
1. Install @vite-pwa/assets-generator:
   ```bash
   cd app && npm install -D @vite-pwa/assets-generator
   ```

2. Create `app/pwa-assets.config.ts` with:
   - Import `combinePresetAndAppleSplashScreens`, `defineConfig`, `minimal2023Preset` from `@vite-pwa/assets-generator/config`
   - Configure splash screen generation:
     - padding: 0.3
     - resizeOptions.background: '#0F172A' (surface color per user decision)
     - resizeOptions.fit: 'contain'
     - linkMediaOptions: log=true, addMediaScreen=true, basePath='/', xhtml=false
   - headLinkOptions preset: '2023'
   - images: ['public/icon.svg']
   - Use the exact config from RESEARCH.md Pattern 4

3. Run the generator: `npx pwa-assets-generator --config pwa-assets.config.ts` from app/ directory. Capture the link tag output from terminal.

4. Update `app/index.html`:
   a. Add `viewport-fit=cover` to the viewport meta tag (required for safe areas in Plan 02):
      `content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"`
   b. Add `<meta name="apple-mobile-web-app-title" content="Logros" />`
   c. Add `<link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" />`
   d. Add all `<link rel="apple-touch-startup-image" ...>` tags output by the generator
      At minimum, include iPhone 11 portrait (828x1792, device-width:414px, device-height:896px, dpr:2)
   e. Keep all existing meta tags (theme-color, apple-mobile-web-app-capable, status-bar-style)

5. Update `app/vite.config.ts`:
   a. Update `includeAssets` to: `['apple-touch-icon.png', 'icon.svg']` (remove non-existent favicon.ico and masked-icon.svg)
   b. Update manifest `icons` array to include:
      - `{ src: 'pwa-192x192.png', sizes: '192x192', type: 'image/png' }`
      - `{ src: 'pwa-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' }`
      - Keep the existing `icon.svg` entry
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Goal Tracker/app" && grep -q "viewport-fit=cover" index.html && grep -q "apple-touch-icon" index.html && grep -q "apple-touch-startup-image" index.html && grep -q "apple-mobile-web-app-title" index.html && grep -q "pwa-192x192" vite.config.ts && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Splash screen PNGs generated for all iOS devices, index.html has viewport-fit=cover + apple-touch-icon link + splash screen links + app title meta, vite.config manifest references PNG icons, app builds without errors</done>
</task>

</tasks>

<verification>
- `app/public/icon.svg` contains `xmlns="http://www.w3.org/2000/svg"` (no MathML typo)
- `app/public/apple-touch-icon.png` exists and is 180x180
- `app/index.html` contains `viewport-fit=cover`, `apple-touch-icon`, at least one `apple-touch-startup-image`
- `app/vite.config.ts` manifest includes `pwa-192x192.png` and `pwa-512x512.png`
- `npx vite build` succeeds
</verification>

<success_criteria>
- Gold trophy icon SVG replaces broken icon, PNGs generated at 180/192/512
- Splash screen images generated for iOS devices (at least iPhone 11)
- index.html fully configured for iOS PWA (viewport-fit, icon, splash, title)
- vite.config manifest updated with PNG icons
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-ios-platform/02-01-SUMMARY.md`
</output>
