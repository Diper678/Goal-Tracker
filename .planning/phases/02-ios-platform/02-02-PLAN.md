---
phase: 02-ios-platform
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/index.css
  - app/src/components/AchievementCard.tsx
  - app/src/App.tsx
autonomous: true
requirements: [IOS-01, IOS-04, IOS-05]

must_haves:
  truths:
    - "User can use the full app on iPhone 11 without content hidden behind notch or home indicator"
    - "User cannot trigger rubber-band overscroll bounce when scrolling to top or bottom"
    - "User can tap every button and toggle without precise aiming (all targets 44px+)"
  artifacts:
    - path: "app/src/index.css"
      provides: "Safe area padding on body + two-layer overscroll lock on html/body"
      contains: "env(safe-area-inset-top"
    - path: "app/src/components/AchievementCard.tsx"
      provides: "44px+ touch targets on toggle, edit, delete buttons"
      contains: "min-w-[44px] min-h-[44px]"
    - path: "app/src/App.tsx"
      provides: "44px+ touch targets on filter buttons"
      contains: "min-h-[44px]"
  key_links:
    - from: "app/index.html"
      to: "app/src/index.css"
      via: "viewport-fit=cover enables env(safe-area-inset-*) to return non-zero values"
      pattern: "env\\(safe-area-inset"
    - from: "app/src/index.css html position:fixed"
      to: "app/src/index.css body overflow-y:auto"
      via: "Two-layer scroll lock: fixed viewport + scrollable body"
      pattern: "position:\\s*fixed"
---

<objective>
Apply iOS platform CSS hardening (safe area insets, overscroll bounce prevention) and fix all interactive element touch targets to meet Apple HIG 44px minimum.

Purpose: The app currently has no safe area awareness (content can overlap the notch/home bar in standalone mode), allows rubber-band bounce scrolling, and has action buttons at 40px (4px under the 44px HIG minimum). This plan makes the app feel like a native iOS dashboard.

Output: Updated index.css with safe areas + scroll lock, AchievementCard.tsx and App.tsx with 44px+ touch targets.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ios-platform/02-CONTEXT.md
@.planning/phases/02-ios-platform/02-RESEARCH.md
@.planning/phases/02-ios-platform/02-01-SUMMARY.md

@app/src/index.css
@app/src/components/AchievementCard.tsx
@app/src/App.tsx

<interfaces>
<!-- From index.css, current body styles (line 47-51): -->
<!-- body { @apply bg-background text-foreground; background-color: #020617; color: #E2E8F0; font-family: 'Fira Sans', ... } -->
<!-- Must ADD safe area padding + overscroll properties to existing body block -->
<!-- Must ADD html scroll lock rules -->

<!-- From AchievementCard.tsx, current button classes (lines 86, 98, 105): -->
<!-- All three action buttons use: w-10 h-10 (= 40px, 4px short of 44px) -->
<!-- Change to: min-w-[44px] min-h-[44px], remove w-10 h-10 -->

<!-- From App.tsx, filter buttons (line 95): -->
<!-- px-4 py-2 text-sm — approx 36px tall, 8px short of 44px -->
<!-- Change to: px-4 min-h-[44px] text-sm -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safe area insets and overscroll lock to index.css</name>
  <files>app/src/index.css</files>
  <action>
Modify `app/src/index.css` to add iOS platform CSS. All changes go in the existing `@layer base` block.

1. Add html rules for the two-layer overscroll prevention (BEFORE the existing body rule):
```css
html {
  position: fixed;
  width: 100%;
  height: 100%;
  overflow: hidden;
  overscroll-behavior: none;
}
```

2. Modify the existing body rule to add scroll lock + safe area padding. Keep all existing properties (`@apply bg-background text-foreground`, background-color, color, font-family) and ADD:
```css
body {
  /* existing properties stay */
  position: relative;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  overscroll-behavior-y: none;
  /* Safe area insets — requires viewport-fit=cover in index.html (added in Plan 01) */
  padding-top: env(safe-area-inset-top, 0px);
  padding-bottom: env(safe-area-inset-bottom, 0px);
  padding-left: env(safe-area-inset-left, 0px);
  padding-right: env(safe-area-inset-right, 0px);
}
```

3. Per user decision: "Hard lock — no bounce at all. Content stops dead at the edges." and "No scroll if content fits the viewport. Lock the viewport when all content is visible."

4. IMPORTANT per RESEARCH.md Pitfall 3: The AchievementForm is already a `position: fixed` full-screen sheet. The scroll lock should not affect it since fixed-position elements are positioned relative to the viewport, not the html element. If testing reveals issues, scope the `position: fixed` on html to `@media (display-mode: standalone)` only.

Do NOT remove or modify any existing CSS rules (scrollbar, selection, focus, animations, pixel-corners, scanlines).
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Goal Tracker/app" && grep -q "env(safe-area-inset-top" src/index.css && grep -q "overscroll-behavior: none" src/index.css && grep -q "position: fixed" src/index.css && grep -q "overflow-y: auto" src/index.css && echo "CSS platform rules present"</automated>
  </verify>
  <done>index.css contains: html position:fixed + overscroll-behavior:none, body with overflow-y:auto + safe-area-inset padding on all four sides, -webkit-overflow-scrolling:touch</done>
</task>

<task type="auto">
  <name>Task 2: Fix touch targets to 44px minimum in AchievementCard and App</name>
  <files>app/src/components/AchievementCard.tsx, app/src/App.tsx</files>
  <action>
1. In `app/src/components/AchievementCard.tsx`, update the three action buttons (toggle, edit, delete):
   - Replace `w-10 h-10` with `min-w-[44px] min-h-[44px]` on all three buttons
   - Keep all other classes unchanged (flex, items-center, justify-center, border, colors, transitions)
   - Per user decision: "Don't change visual design, just ensure hit areas are comfortable"
   - The 4px increase (40px to 44px) is invisible on the dark theme

   Specific changes:
   - Line ~86: Toggle button `w-10 h-10` -> `min-w-[44px] min-h-[44px]`
   - Line ~98: Edit button `w-10 h-10` -> `min-w-[44px] min-h-[44px]`
   - Line ~105: Delete button `w-10 h-10` -> `min-w-[44px] min-h-[44px]`

2. In `app/src/App.tsx`, update the filter buttons:
   - Line ~95: Replace `px-4 py-2` with `px-4 min-h-[44px]` on filter buttons (Todos/Completados/Pendientes)
   - The buttons gain ~8px height (from ~36px to 44px) — ensures comfortable tapping
   - Keep all other classes (text-sm, font-mono, uppercase, transition, colors)
   - The "NUEVO LOGRO" button is already ~48px tall via `py-3` — no change needed

3. Per RESEARCH.md audit:
   - Toggle button: 40px -> 44px (was 2px SHORT per dimension)
   - Edit button: 40px -> 44px (was 2px SHORT)
   - Delete button: 40px -> 44px (was 2px SHORT)
   - Filter buttons: ~36px -> 44px (was ~8px SHORT)
   - "NUEVO LOGRO": ~48px (PASSES, no change)
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/Goal Tracker/app" && grep -c "min-w-\[44px\] min-h-\[44px\]" src/components/AchievementCard.tsx | grep -q "3" && grep -q "min-h-\[44px\]" src/App.tsx && npx vite build --mode development 2>&1 | tail -3</automated>
  </verify>
  <done>All three AchievementCard action buttons are min-w-[44px] min-h-[44px], filter buttons in App.tsx are min-h-[44px], app builds without errors</done>
</task>

</tasks>

<verification>
- `app/src/index.css` html block has `position: fixed` + `overscroll-behavior: none`
- `app/src/index.css` body block has `overflow-y: auto` + `env(safe-area-inset-*)` padding
- `app/src/components/AchievementCard.tsx` has exactly 3 occurrences of `min-w-[44px] min-h-[44px]`
- `app/src/App.tsx` filter buttons have `min-h-[44px]`
- `npx vite build` succeeds
- No existing CSS rules were removed
</verification>

<success_criteria>
- Safe area padding active via env() on all four sides of body
- Two-layer overscroll bounce prevention in place (fixed html + scrollable body)
- All interactive buttons meet 44px minimum (3 card buttons + filter buttons)
- App builds and renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-ios-platform/02-02-SUMMARY.md`
</output>
